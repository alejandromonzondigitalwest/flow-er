export class Parser {

   constructor(_id = '', parser = '', version = '') {
        this._id = _id;
        this.parser = parser;
        this.version = version;    }

    _id: string;
    parser: string;
    version: string;    
    }
